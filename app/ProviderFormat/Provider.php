<?php

/**
 * this provider responsible for convert objects in json files to specified format ue define like [array, collection, ...]
 */

namespace App\ProviderFormat;

interface Provider
{
    public function transactions();
}
